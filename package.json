{
  "private": true,
  "name": "@cameronhunter/with-timeout",
  "version": "0.0.0",
  "license": "MIT",
  "main": "./build/withTimeout.js",
  "types": "./build/withTimeout.d.ts",
  "scripts": {
    "build": "tsc -p ./tsconfig.build.json",
    "start": "ts-node $@",
    "test": "vitest"
  },
  "devDependencies": {
    "@tsconfig/node-lts-strictest": "^18.12.1",
    "@types/node": "~18",
    "typescript": "^5.1.6",
    "vitest": "^0.33.0"
  }
}
